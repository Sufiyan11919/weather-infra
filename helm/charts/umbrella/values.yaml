replicaCount: 2

image:
  repository: ""
  tag: ""

rollout:
  enabled: true          # prod keeps blue/green; UAT overrides to false
  strategy: blueGreen
  activeService: blue
  previewService: green

service:
  type: ClusterIP
  port: 4000

ingress:
  enabled: true
  className: nginx
  hosts: []              # supply per env if you need hostnames
