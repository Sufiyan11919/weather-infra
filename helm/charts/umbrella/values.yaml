replicaCount: 2

image:
  repository: ""
  tag: ""

service:
  type: ClusterIP
  port: 4000            # default; each env file can override

ingress:
  enabled: true
  className: nginx
  hosts: []
  # each env file supplies its own host list

rollout:
  strategy: blueGreen
  activeService: blue
  previewService: green
