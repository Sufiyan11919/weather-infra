name: Promote GA via ECR
on:
  push:
    # only non-rc tags, e.g. v1.4.0
    tags:
      - 'v*.*.*'

env:
  AWS_REGION: us-east-2
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
  SERVICES: favourite-api weather-api weather-frontend
  GA_TAG: ${{ github.ref_name }}       # e.g. "v1.4.0"

jobs:
  retag-and-bump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout infra repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Retag latest RC images to GA (skip if already exists)
        run: |
          set -euo pipefail

          for svc in $SERVICES; do
            echo "🔍 Checking for existing GA tag $GA_TAG in $svc..."
            GA_EXISTS=$(aws ecr list-images \
              --repository-name "$svc" \
              --filter tagStatus=TAGGED \
              --query "imageIds[?imageTag=='${GA_TAG}']" \
              --output json)

            if [ "$GA_EXISTS" != "[]" ]; then
              echo "✅ $svc:$GA_TAG already exists – skipping retag."
              continue
            fi

            echo "⏳ Finding latest RC tag for $svc from UAT values..."
            # Read the RC directly from your UAT values.yaml
            RC_TAG=$(grep '^  tag:' environments/uat/$svc/values.yaml \
                     | awk -F'"' '{print $2}')
            if [[ ! $RC_TAG =~ ^${GA_TAG}-rc\.[0-9]+$ ]]; then
              echo "❌ Unexpected RC tag '$RC_TAG' for $svc – aborting."
              exit 1
            fi

            echo "🔖 Retagging $svc:$RC_TAG → $svc:$GA_TAG"
            aws ecr batch-get-image \
              --repository-name "$svc" \
              --image-ids imageTag="$RC_TAG" \
              --query 'images[0].imageManifest' --output text \
              > manifest.json

            aws ecr put-image \
              --repository-name "$svc" \
              --image-tag "$GA_TAG" \
              --image-manifest file://manifest.json
          done

      - name: Install GitHub CLI
        run: |
          sudo apt-get update -y
          sudo apt-get install -y gh

      - name: Authenticate GH CLI
        run: echo "${{ secrets.GH_PAT }}" | gh auth login --with-token

      - name: Bump prod values & open PR
        run: |
          git checkout -b bump-prod-${GA_TAG}

          for svc in $SERVICES; do
            FILE=environments/prod/$svc/values.yaml
            echo "✏️  Updating $FILE → tag: ${GA_TAG}"
            sed -i "s|tag:.*|tag: \"${GA_TAG}\"|" "$FILE"
          done

          git commit -am "chore: promote services → ${GA_TAG}"
          git push -u origin bump-prod-${GA_TAG}

          gh pr create \
            --title  "Promote all services → ${GA_TAG}" \
            --body   "Automated GA promotion from infra" \
            --base   main \
            --head   bump-prod-${GA_TAG}
